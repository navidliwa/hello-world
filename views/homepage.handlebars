<div>
    <nav>
        <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        {{#unless logged_in}}
        <a href="/login">Login</a>
        {{/unless}}
        {{#if logged_in}}
        <a href="/">Logout</a>
        {{/if}}
    </nav>
    {{#each posts}}
    <h2>{{title}}</h2>
    <h3>{{body}}</h3>
    {{/each}}
</div>