<div>
    <h2>{{post.title}}</h2>
    <h3>{{post.body}}</h3>
    <p>Comments:</p>
    {{#if post.Comments}}
    {{#each post.Comments}}
    {{body}}
    <p>From:</p>
    {{user.username}}
    {{/each}}
    {{/if}}
</div>